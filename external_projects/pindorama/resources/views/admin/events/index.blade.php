@extends('layouts.dashboard')
@section('title','Eventos')
@section('content')
<div class="mx-auto max-w-5xl"><div class="mb-6 flex items-center justify-between"><div><h1 class="text-2xl font-extrabold">Eventos e cursos</h1><p class="text-sm text-neutral-500">Programação controlada exclusivamente pelo root.</p></div><a href="{{ route('admin.events.create') }}" class="btn-brand">Novo evento</a></div>
<div class="card overflow-x-auto"><table class="w-full text-sm"><thead><tr class="border-b text-left text-xs uppercase text-neutral-400"><th class="p-3">Evento</th><th>Data</th><th>Instrutores</th><th>Inscritos</th><th>Status</th><th></th></tr></thead><tbody>@forelse($events as $event)<tr class="border-b"><td class="p-3 font-bold">{{ $event->title }}</td><td>{{ $event->starts_at->setTimezone($event->timezone)->format('d/m/Y H:i') }}</td><td class="text-neutral-500">{{ $event->instructors->pluck('display_name')->join(', ') }}</td><td>{{ $event->taken }}@if($event->capacity)/{{ $event->capacity }}@endif</td><td><span class="chip bg-neutral-100">{{ \App\Models\Event::STATUSES[$event->status] }}</span></td><td class="whitespace-nowrap text-right"><a href="{{ route('admin.events.registrations',$event) }}" class="text-brand-700">inscritos</a> · <a href="{{ route('admin.events.edit',$event) }}" class="text-brand-700">editar</a></td></tr>@empty<tr><td colspan="6" class="p-6 text-center text-neutral-500">Nenhum evento.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $events->links() }}</div></div>
@endsection
