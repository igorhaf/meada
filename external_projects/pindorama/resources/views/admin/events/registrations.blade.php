@extends('layouts.dashboard')
@section('title','Inscritos · '.$event->title)
@section('content')
<div class="mx-auto max-w-5xl"><div class="flex justify-between"><div><a href="{{ route('admin.events.index') }}" class="text-sm text-neutral-500">← Eventos</a><h1 class="mt-2 text-2xl font-extrabold">Inscritos · {{ $event->title }}</h1></div><a href="{{ route('admin.customers.create') }}" class="btn-outline self-start">Novo cliente</a></div>
<form method="POST" action="{{ route('admin.events.registrations.store',$event) }}" class="card mt-5 grid gap-3 p-5 sm:grid-cols-4">@csrf<select name="customer_id" required class="rounded-xl border px-3 py-2 text-sm"><option value="">Selecione o aluno</option>@foreach($customers as $customer)<option value="{{ $customer->id }}">{{ $customer->name }} · {{ $customer->email }}</option>@endforeach</select><input name="participant_name" placeholder="Nome do participante" required class="rounded-xl border px-3 py-2 text-sm"><input name="participant_phone" placeholder="Telefone" class="rounded-xl border px-3 py-2 text-sm"><div><label class="mr-2 text-xs"><input type="checkbox" name="mark_paid" value="1"> pago manualmente</label><label class="block text-xs"><input type="checkbox" name="privacy_consent" value="1" required> consentimento confirmado</label><button class="btn-brand mt-1">Matricular</button></div></form>
<div class="card mt-5 overflow-x-auto"><table class="w-full text-sm"><thead><tr class="border-b text-left text-xs uppercase text-neutral-400"><th class="p-3">Participante</th><th>Contato</th><th>Status</th><th>Pagamento</th><th>Passaporte</th></tr></thead><tbody>@forelse($registrations as $r)<tr class="border-b"><td class="p-3 font-semibold">{{ $r->participant_name }}</td><td class="text-neutral-500">{{ $r->participant_email }}<br>{{ $r->participant_phone }}</td><td>{{ $r->status_label }}</td><td>{{ $r->payment_status }} · {{ money($r->amount) }}</td><td>@if($pass=$r->accessPasses->first())<a href="{{ URL::temporarySignedRoute('passes.show',now()->addYear(),['pass'=>$pass]) }}" class="text-brand-700">{{ $pass->public_code }}</a>@else—@endif</td></tr>@empty<tr><td colspan="5" class="p-6 text-center text-neutral-500">Nenhum inscrito.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $registrations->links() }}</div></div>
@endsection
