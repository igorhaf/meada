# docker-compose.override.yml — overrides de DESENVOLVIMENTO LOCAL do meadadigital.
#
# Carregado automaticamente em `docker compose up` (dev). Em PRODUÇÃO o deploy usa
# `docker compose -f docker-compose.yml up` (só a base = modo prod).
#
# Só cobre os serviços do PRÓPRIO meada (backend/frontend). Os projetos externos e o
# Caddy/cloudflared têm os seus próprios composes/overrides (ver shared e cada repo).
services:
  backend:
    build:
      target: dev
    volumes:
      - ./src:/app/src
      - ./pom.xml:/app/pom.xml
      - ./supabase:/app/supabase
      - maven-cache:/root/.m2
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://${SUPABASE_HOST_IP:-172.27.153.135}:54322/postgres
      - SUPABASE_JWKS_URL=http://${SUPABASE_HOST_IP:-172.27.153.135}:54321/auth/v1/.well-known/jwks.json
      - SUPABASE_URL=http://${SUPABASE_HOST_IP:-172.27.153.135}:54321
      - SUPABASE_ISSUER=http://127.0.0.1:54321/auth/v1
      - ADMIN_CORS_ALLOWED_ORIGINS=http://meadadigital.local,http://*.meadadigital.local,http://localhost:3000
      # Evolution LOCAL (~/shared/evolution-local, host :8086). O backend em container
      # alcança o host (WSL) pelo host.docker.internal.
      - EVOLUTION_BASE_URL=http://host.docker.internal:8086
      - EVOLUTION_GLOBAL_API_KEY=${EVOLUTION_GLOBAL_API_KEY:-}
      - EVOLUTION_WEBHOOK_URL=http://host.docker.internal:8095/webhooks/evolution

  frontend:
    build:
      target: dev
    env_file: ./frontend/.env.local
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NEXT_PUBLIC_API_URL=http://api.meadadigital.local
      - WATCHPACK_POLLING=true
      - NEXT_PUBLIC_SUPABASE_URL=http://${SUPABASE_HOST_IP:-172.27.153.135}:54321

volumes:
  maven-cache:
